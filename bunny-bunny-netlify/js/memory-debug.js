import { escapeHtml, showToast, openSheet, closeSheet } from "./core/ui.js";
import { memoryProfile, listMemoryEntries, clearCharacterMemory, retrieveMemoryContext } from "./memory-engine.js";

export function createMemoryDebugRenderer({store,navigate}){return async function render(container,params={}){const state=store.getState(),personId=params.personId||state.people.find(x=>x.type==="char")?.id,person=state.people.find(x=>x.id===personId);if(!person)return navigate("contacts");const profile=memoryProfile(state,personId),entries=await listMemoryEntries(personId);container.className="app-view memory-debug-view";container.innerHTML=`<section class="memory-debug-hero"><span>PRIVATE MIND / DEBUG</span><h2>${escapeHtml(person.name)} 的记忆库</h2><p>这里只用于本机调试，不会作为聊天内容展示给角色。</p></section><section class="memory-state-grid"><article><span>关系阶段</span><strong>${escapeHtml(profile.dynamicState.relationshipStage.stage)}</strong></article><article><span>信任</span><strong>${profile.dynamicState.charImpressionOfUser.trustLevel}</strong></article><article><span>好感</span><strong>${profile.dynamicState.charImpressionOfUser.affectionLevel}</strong></article></section><section class="card stack"><div class="section-title"><h3>核心记忆</h3><span>${entries.length} 条向量/事件记录</span></div><p class="memory-core">${escapeHtml(profile.coreMemory)}</p><pre>${escapeHtml(JSON.stringify(profile.dynamicState,null,2))}</pre></section><div class="row"><button class="button secondary" data-test-retrieval>测试检索</button><button class="button secondary" data-clear-memory>清空记忆</button></div><div class="section-title"><h3>最近实际提示词</h3><span>${profile.lastPromptAt?new Date(profile.lastPromptAt).toLocaleString("zh-CN"):"尚未记录"}</span></div><pre class="prompt-inspector">${escapeHtml(profile.lastPrompt||"请先在聊天设置开启“提示词测试模式”，然后与角色对话一次。")}</pre><div class="section-title"><h3>记忆条目</h3><span>IndexedDB</span></div><section class="memory-entry-list">${entries.sort((a,b)=>b.timestamp-a.timestamp).map(x=>`<article><span>${label(x.kind)}</span><p>${escapeHtml(x.text||x.event||"")}</p><small>${new Date(x.timestamp).toLocaleString("zh-CN")}</small></article>`).join("")||'<div class="empty"><strong>还没有形成长期记忆</strong></div>'}</section>`;container.querySelector("[data-test-retrieval]").onclick=()=>openSheet(`<div class="sheet-title"><h3>测试记忆检索</h3><button class="button ghost" data-sheet-close>关闭</button></div><label class="field"><span>模拟 USER 消息</span><textarea data-query rows="4" placeholder="例如：你还记得我不吃香菜吗？"></textarea></label><button class="button" data-run>查看命中</button><pre class="retrieval-result" data-result></pre>`,{onReady(sheet){sheet.querySelector("[data-run]").onclick=async()=>{const result=await retrieveMemoryContext(store.getState(),personId,sheet.querySelector("[data-query]").value);sheet.querySelector("[data-result]").textContent=JSON.stringify({keyword:result.keyword,episodic:result.episodic,semantic:result.semantic},null,2)}}});container.querySelector("[data-clear-memory]").onclick=()=>{openSheet(`<div class="confirm-dialog"><h3>清空 ${escapeHtml(person.name)} 的记忆？</h3><p>角色档案会保留，向量记忆、事件记忆和动态关系状态会被重置。</p><div class="row"><button class="button secondary" data-sheet-close>取消</button><button class="button" data-confirm>清空</button></div></div>`,{onReady(sheet){sheet.querySelector("[data-confirm]").onclick=async()=>{await clearCharacterMemory(personId);store.update(s=>{delete s.memoryProfiles?.[personId]});closeSheet();showToast("记忆库已清空");render(container,params)}}})}}}
function label(kind){return({longTerm:"长期记忆",episodic:"情景记忆",keyword:"关键词记忆"})[kind]||kind}
